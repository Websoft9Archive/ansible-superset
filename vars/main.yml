#全局
region: "0"

nginx_certbot: True
nginx_reverse_proxy_port: "8088"

# docker
docker_applications: 'phppgadmin'
  
# 应用数据库信息
init_application:
  rabbitmq:
    service: rabbitmq
    database: rabbitmq 
    database_user: rabbitmq
    database_host: localhost
    database_password: "123456"
    config_path:
      - /opt/rabbitmq/config/config.json
